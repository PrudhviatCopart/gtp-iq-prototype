services:
  - type: web
    name: cfc-instaquote-mcp
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: npm run mcp
    envVars:
      - key: NODE_VERSION
        value: 20
